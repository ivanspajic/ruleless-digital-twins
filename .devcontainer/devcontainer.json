{
    "name": "SmartNode",
    "build": { "dockerfile": "Dockerfile",
    "context": "../SmartNode"
    },
    "postCreateCommand": "pip install --user --break-system-packages pythonfmu fmpy pytz requests_cache python-dateutil",
    "customizations": {
        "vscode": {
            "extensions": ["ms-dotnettools.csdevkit"]
        }
    }
}